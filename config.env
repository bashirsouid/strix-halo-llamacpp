# =============================================================================
# Strix Halo — llama.cpp (Vulkan RADV) configuration
# =============================================================================

# Docker image tag for kyuz0's toolbox
# Some models perform faster on one or the other; test both and set this to the best for your use case.
LLAMA_IMAGE_TAG=vulkan-radv
###LLAMA_IMAGE_TAG=vulkan-amdvlk
###LLAMA_IMAGE_TAG=rocm-6.4.4-rocwmma

# ── Paths ─────────────────────────────────────────────────────────────────────
# Primary models directory (HF safetensors, GGUFs, etc.)
MODELS_DIR=/mnt/data/models

# llama-server download + HF cache (persistent across container restarts)
HOST_LLAMA_CACHE=/mnt/data/llama.cpp-cache

# Extra model search directories (colon-separated)
# lib.sh already searches the most common locations automatically.
# Add unusual paths here — e.g. a second drive or a shared NAS mount.
EXTRA_MODEL_SEARCH_DIRS=

# ── Server defaults ───────────────────────────────────────────────────────────
SERVER_PORT=8000

# ── Default model (used by start.sh on boot) ──────────────────────────────────
### Nemotron Nano 8B (GGUF) — a tiny, efficient LLaMA 3.1 variant with 8-bit quantization, ideal for coding tasks ###
# # DEFAULT_LOADER=load_nemotron_nano.sh
# # MODEL_HF_ID=bartowski/nvidia_Llama-3.1-Nemotron-Nano-8B-v1-GGUF
# # MODEL_KEYWORD=nemotron-nano-8b
# # MODEL_ALIAS=nemotron-nano-8b
# # PREF_QUANT=Q8_0

# # ### DeepSeek Coder V2 Lite (GGUF) — a compact, efficient LLaMA 3.1 variant optimized for coding tasks, with 8-bit quantization and a large context window ###
# # DEFAULT_LOADER=load_deepseek-coder-v2-lite.sh
# # MODEL_HF_ID=bartowski/DeepSeek-Coder-V2-Lite-Instruct-GGUF
# # MODEL_KEYWORD=deepseek-coder-v2-lite
# # MODEL_ALIAS=deepseek-coder-v2-lite
# # PREF_QUANT=Q8_0_L

### Mistral Small 4 (GGUF) — a compact, efficient LLaMA 3.1 variant with 4-bit quantization, ideal for general-purpose use ###
DEFAULT_LOADER=load_mistral-small-4_q4.sh
MODEL_HF_ID=unsloth/Mistral-Small-4-119B-2603-GGUF
MODEL_KEYWORD=mistral-small-4
MODEL_ALIAS=mistral-small-4
PREF_QUANT=UD-Q4_K_M

# ### GLM-4.5-Air (GGUF) — a LLaMA 4.5 variant with 4-bit quantization, optimized for instruction-following and tool use ###
# # DEFAULT_LOADER=load_glm_4.5_air.sh
# # MODEL_HF_ID=unsloth/GLM-4.5-Air-GGUF
# # MODEL_KEYWORD=glm-4.5-air
# # MODEL_ALIAS=glm-4.5-air
# # PREF_QUANT=IQ4_XS

# ### Devstral Small 2 (GGUF) — a compact, efficient LLaMA 3.1 variant with 8-bit quantization, ideal for coding tasks
# # # DEFAULT_LOADER=load_devstral-small-2.sh
# # # MODEL_HF_ID=unsloth/Devstral-Small-2-24B-Instruct-2512-GGUF
# # # MODEL_KEYWORD=devstral-small-2-24b
# # # MODEL_ALIAS=devstral-small-2-24b
# # # PREF_QUANT=Q8_0

# ── llama-server tuning ───────────────────────────────────────────────────────
#LLAMA_CTX_SIZE=8192 
LLAMA_NGL=999       # number of layers on GPU (999 = all)
LLAMA_THREADS=1     # CPU threads — Strix Halo has 16c/32t, 16 is a good start
LLAMA_TEMP=0.2
LLAMA_TOP_P=0.95

# ── Model search — overrides (optional) ──────────────────────────────────────
# These are set at runtime by load_*.sh scripts; leave blank here.
MODEL_FLAG=
MODEL_VALUE=
